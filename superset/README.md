### SQL Expressions for charts

**Calculate duration between 2 dates as number of days**
```
DATEDIFF(NOW(), hire_date)
```

##### Convert column into temporal format natively
```
CAST (birth_date AS DATE)
```
